// Catch-all route so deep links/refreshes render the shell instead of Next.js 404.
// Shell renders the explore view via InitView in petblack-effect-atom.
export default function CatchAllPage() {
    return null;
}
